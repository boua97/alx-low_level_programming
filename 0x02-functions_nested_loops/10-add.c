#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 *
 * @i: input number
 * @n: input number
 *
 * Return: return value of add
*/

int add(int i, int n)
{
	i = i + n;

	return (i);
}
